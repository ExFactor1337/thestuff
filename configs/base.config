params { 
    definitions {
        input_file {  
            default_value = "tests/input_file.txt"
            type = "path"
            description = "Path params are strings evaluated as paths - they are checked for existence before being passed on." 
            required = false
        }
        help {
            type = "flag" 
            description = "Print this help message and exit."
            required = false
        }
    }
}



