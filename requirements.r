# Hashtag comments are OK in this file
# R requirement files don't need versions, as they are tied to a static MRAN snapshot 
# MRAN snapshot is tied to the rocker version, so there is your version control
dplyr
ggplot2
data.table
